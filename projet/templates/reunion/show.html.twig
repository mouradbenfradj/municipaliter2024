{% extends 'base.html.twig' %}

{% block title %}Reunion{% endblock %}

{% block body %}
    <h1>Reunion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reunion.id }}</td>
            </tr>
            <tr>
                <th>الموضوع</th>
                <td>{{ reunion.sujet }}</td>
            </tr>
            <tr>
                <th>الوصف</th>
                <td>{{ reunion.description }}</td>
            </tr>
            <tr>
                <th>تاريخ البدء</th>
                <td>{{ reunion.date ? reunion.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{{ reunion.datefin ? reunion.datefin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reunion_index') }}">back to list</a>

    <a href="{{ path('app_reunion_edit', {'id': reunion.id}) }}">edit</a>

    {{ include('reunion/_delete_form.html.twig') }}
{% endblock %}
